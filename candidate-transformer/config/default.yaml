application:
  name: "Candidate Intelligence Transformation Engine"
  environment: "local"

pipeline:
  current_sprint: "Phase 1"
  status: "Foundation ready"
  stages:
    - initialize
    - ingest
    - map
    - normalize
    - group
    - merge
    - project
    - validate
    - export

paths:
  inputs: "inputs"
  outputs: "outputs"
  logs: "logs"
