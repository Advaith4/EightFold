logging:
  level: "INFO"
  directory: "logs"
  file_name: "application.log"
  rotation: "00:00"
  retention: "14 days"
  compression: "zip"
  format: "{time:YYYY-MM-DD HH:mm:ss.SSS} | {level} | {name}:{function}:{line} | {message}"
